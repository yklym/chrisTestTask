export enum UserFormStep {
  STEP_ONE = 0,
  STEP_TWO = 1,
  STEP_THREE = 2,
}

export enum UserFormFieldName {
  fullName = "fullName",
  email = "email",
  phone = "phone",
  annualRevenue = "annualRevenue",
  fundingRequestedAmount = "fundingRequestedAmount",
}
